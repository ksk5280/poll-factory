<%= link_to "Create a New Poll", new_poll_path, class: "btn btn-default", id: "new-poll-btn" %>

<div class="list-group">
  <% @polls.each do |poll| %>
    <%= link_to "#{poll.question}", poll_path(poll), class: "list-group-item poll-list" %>
  <% end %>
</div>
